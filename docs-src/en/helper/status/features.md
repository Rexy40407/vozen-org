---
title: Feature maturity and availability
---
# Feature maturity and availability

<div class="status-summary"><span class="status status-operational">Available {{ helperDocs.features | selectattr('maturity','equalto','operational') | list | length }}</span><span class="status status-beta">Beta {{ helperDocs.features | selectattr('maturity','equalto','beta') | list | length }}</span><span class="status status-blocked">Unavailable {{ helperDocs.features | selectattr('maturity','equalto','blocked') | list | length }}</span></div>
<p>These badges come from the technical manifest generated by Vozen Helper. Per-server health and permissions are checked separately.</p>
<div class="module-index">{% for feature in helperDocs.features %}<a class="module-row" href="/docs/helper/modules/{{ feature.slug }}/"><span><strong>{{ feature.title }}</strong><small>{{ feature.key }}</small></span><span class="status status-{{ feature.maturity }}">{{ feature.statusLabel }}</span></a>{% endfor %}</div>

