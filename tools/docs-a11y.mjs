import fs from 'node:fs';import path from 'node:path';const root=path.resolve('site/docs/helper');const files=[];function walk(dir){for(const e of fs.readdirSync(dir,{withFileTypes:true})){const f=path.join(dir,e.name);if(e.isDirectory())walk(f);else if(e.name.endsWith('.html'))files.push(f)}}walk(root);const errors=[];for(const f of files){const h=fs.readFileSync(f,'utf8');if(!/<html[^>]+lang="en"/i.test(h))errors.push(`${f}: missing lang`);if(!h.includes('class="skip-link"'))errors.push(`${f}: missing skip link`);if(!/<main\b/i.test(h))errors.push(`${f}: missing main`);if(!/<h1\b/i.test(h))errors.push(`${f}: missing h1`);for(const m of h.matchAll(/<input\b([^>]*)>/gi)){if(!/aria-label=|id="docs-search"/.test(m[1]))errors.push(`${f}: unlabeled input`)}}if(errors.length)throw new Error(errors.join('\n'));console.log(`Accessibility smoke check passed for ${files.length} pages`);
